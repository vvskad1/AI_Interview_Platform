# Environment variables for production build (AWS Full Stack)
# The frontend will be served from the same domain as backend
# Leave empty to use same origin, or set to your backend URL
VITE_API_BASE_URL=
